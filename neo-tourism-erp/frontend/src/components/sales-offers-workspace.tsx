"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { apiFetch } from "@/lib/api/client";
import type { MarketingDeal } from "@/types/marketing";
import { Badge,date,dateTime } from "./marketing-deals-workspace";
export function SalesOffersWorkspace(){const [deals,setDeals]=useState<MarketingDeal[]>([]);const [error,setError]=useState("");useEffect(()=>{void apiFetch<MarketingDeal[]>("/marketing/deals/sales/available").then(setDeals).catch(e=>setError(e instanceof Error?e.message:"Unable to load offers."))},[]);return <main className="mx-auto max-w-7xl px-5 py-8 sm:px-8"><p className="text-sm font-bold uppercase tracking-[.2em] text-cyan-700">Sales desk</p><h1 className="mt-1 text-3xl font-semibold">Latest Approved Offers</h1><p className="mt-2 text-sm text-slate-600">Only approved, active and unexpired Deal Cards are shown.</p>{error&&<p className="mt-5 rounded-xl bg-red-50 p-4 text-red-700">{error}</p>}<div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{deals.map(d=><article key={d.id} className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex justify-between gap-3"><p className="font-mono text-xs text-slate-500">{d.dealCode}</p><Badge value={d.status}/></div><h2 className="mt-4 text-2xl font-semibold">{d.destination}</h2><p className="mt-2 text-4xl font-bold text-cyan-800">{d.currency} {d.price}</p><dl className="mt-5 space-y-3 text-sm"><div><dt className="text-slate-500">Travel</dt><dd>{date(d.travelStartDate)} – {date(d.travelEndDate)}</dd></div><div><dt className="text-slate-500">Departure</dt><dd>{d.departureLocation}</dd></div><div><dt className="text-slate-500">Baggage</dt><dd>{d.baggage??"—"}</dd></div><div><dt className="text-slate-500">Expires</dt><dd>{dateTime(d.expiryAt)}</dd></div></dl><Link href={`/sales/offers/${d.id}`} className="mt-6 block rounded-xl bg-cyan-700 px-4 py-3 text-center font-semibold text-white">View Offer</Link></article>)}</div>{!deals.length&&!error&&<p className="mt-8 rounded-2xl border bg-white p-10 text-center text-slate-500">No approved live offers are currently available.</p>}</main>}
